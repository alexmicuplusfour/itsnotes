/**
 * useNavigation
 *
 * Convenience hook for accessing navigation state and actions.
 * Re-exports useNavigationContext with a shorter name.
 *
 * Usage:
 * const { state, openNote, closeNote } = useNavigation();
 */

export { useNavigationContext as useNavigation } from './NavigationContext';
